VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Absolute URL of the deployed backend API (e.g. https://gig-finder-api.onrender.com).
# Leave empty in local dev — Vite's dev server proxy handles /api instead.
VITE_API_URL=
